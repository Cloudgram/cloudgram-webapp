version: '3.8'

services:
    webapp:
        build:
            context: .
            dockerfile: Dockerfile
        restart: always
        depends_on:
            - postgres
        expose:
            - '8002'
